server {
    listen 80;
    server_name autoworkflow.mikotech.vn;

    # ACME Challenge for Let's Encrypt
    location /.well-known/acme-challenge/ {
        alias /var/www/certbot/;
    }

    # Temporary: serve app via HTTP (enable redirect after SSL setup)
    location / {
        proxy_pass http://app:8888;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }

    # Uncomment after SSL setup to redirect HTTP to HTTPS
    # location / {
    #     return 301 https://$server_name$request_uri;
    # }
}

# SSL block - uncomment after getting certificate
# server {
#     listen 443 ssl http2;
#     server_name autoworkflow.mikotech.vn;
#
#     # SSL Configuration
#     ssl_certificate /etc/letsencrypt/live/autoworkflow.mikotech.vn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/autoworkflow.mikotech.vn/privkey.pem;
#
#     # SSL Hardening
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
#     ssl_prefer_server_ciphers off;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_timeout 10m;
#
#     # Security Headers
#     add_header X-Frame-Options DENY;
#     add_header X-Content-Type-Options nosniff;
#     add_header X-XSS-Protection "1; mode=block";
#     add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload";
#
#     # Next.js App Proxy
#     location / {
#         proxy_pass http://app:8888;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#
#         # WebSocket support
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#     }
#
#     # Health check
#     location /health {
#         access_log off;
#         return 200 "healthy\n";
#         add_header Content-Type text/plain;
#     }
# }
